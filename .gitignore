# Real statements / anything derived from real personal data — never commit
/samples/
*.local.yaml
.env
config.yaml
rules.yaml

# Generated output written inside the repo by mistake (e-mail preview, statements)
/preview.html
/statements/
*.statement.pdf

# Runtime data (normally lives outside the repo in %LOCALAPPDATA%\autobill)
/data/
*.db
*.db-wal
*.db-shm

# Python
__pycache__/
*.py[cod]
.venv/
.pytest_cache/
.ruff_cache/

# Docker deployment: the real password file and the data folder next to compose.yaml
/autobill.env
