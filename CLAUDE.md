# CLAUDE.md — AutoBill 项目规则（给 AI 助手）

AutoBill：读取转发到"中心邮箱"的国内信用卡账单邮件（农行 HTML、建行 HTML、中行 PDF），用**纯规则**解析，存进 SQLite，折算成人民币后生成**汇总报表**邮件。自用、公开仓库、Python。

**作者是开发新手**：解释要讲清楚，每一步要能验证，不要一次改很多。

## 开始任何工作前
1. 读 [project.md](project.md)，了解定位、决策、**第一版范围**和风险。
2. 读 [docs/development.md](docs/development.md)，了解流程、里程碑和测试规范，并确认**当前在哪个里程碑**（见下文）。
3. 读和这一步有关的设计文档（development.md 每个里程碑下都有链接）。
4. **先给出计划，等作者确认后再动手。**

**当前里程碑**：M0–M2 已合并（PR #1–#3）。M3 代码已完成（分支 `feat/m3-first-pipeline`），等 CI 变绿、作者自己跑一遍 `import-dir` 和 `report` 后合并；合并后进入 M4（建行解析器）。每完成一步就更新这一行。

## 常用命令
```powershell
uv sync
uv run pytest
uv run ruff check . ; uv run ruff format .
uv run autobill --help
```

## 红线（不要违反）
- **隐私**：
  - 仓库是公开的。**绝不提交**原始账单，或者任何含真实姓名、住址、卡号、证件号、手机号、邮箱/QQ 号的内容。
  - 样本只放在 `tests/fixtures/`，而且必须是已经脱敏的。
  - 隐私边界**只到身份信息**：消费记录、金额、商户、额度可以保留。**不要过度脱敏，也不要新增"脱敏脚本"**，作者明确说过不需要。
- **Git**：
  - 不直接提交到 `main`。每一步开 `feat/…` 分支，一步一个 PR，**由作者合并**。
  - 不要自己 push、合并或打 tag，除非作者明确要求。
- **范围**：只做 project.md 里"第一版"的内容。标着"以后"的功能（outbox、质量优先级、DKIM、心跳、企业微信、Oracle、Beancount），除非作者提出，否则不要主动实现。
- **决策不要推翻**：
  - 不接 AI 解析；
  - 取信方式是中心邮箱加转发（不要改成直接读主邮箱）；
  - 不做还款提醒，只展示还款日；
  - 报表只做汇总，不列逐笔流水；
  - 汇率按账单邮件当天的网上汇率（Frankfurter）。
- **代码**：
  - 金额一律用 `Decimal`；
  - PDF 用 pdfplumber，**不用 PyMuPDF**（AGPL 许可，和公开仓库冲突）；
  - PDF 按内容开头的 `%PDF-` 识别，不看文件后缀；
  - 解析器找不到关键区块时要抛 `TemplateChanged`，**绝不静默返回空结果**；
  - 代码注释和标识符用英文，文档用中文。
  - 源码里的不可见字符（不换行空格、全角空格、BOM、U+2212 减号等）一律写成转义形式（反斜杠加 u 和四位十六进制），不要写真实字符。AI 的写文件工具和命令行会把转义悄悄变成真实字符，写完要检查字节。
- **测试**：
  - 不联网（汇率、SMTP、IMAP 都用假对象代替）；
  - 用 `tmp_path` 作为 `AUTOBILL_DATA_DIR`，不碰真实数据库；
  - 不能为了让测试通过而直接覆盖快照，快照变了要说明原因。
- **文档**：改了代码的行为，就在同一个分支里更新对应的文档（对照表在 project.md）。一个主题只写在一个文件里。

## 文档地图
| 文件 | 内容 |
|---|---|
| `project.md` | 总览、第一版范围、决策、架构、分期、待定事项、风险 |
| `docs/development.md` | 开发流程、里程碑 M0–M8、测试规范、Git、CI、版本发布 |
| `docs/banks/` | 三家银行的格式规格、注册表、样本覆盖矩阵（**写解析器之前必读**） |
| `docs/data-model.md` | 模型、符号约定、分项对账、汇率、SQLite 表 |
| `docs/parsing.md` / `fetcher.md` / `pipeline.md` / `notify.md` | 各模块的设计 |
| `docs/security.md` | 密钥、配置、数据隔离、新样本脱敏检查清单 |
| `docs/setup.md` | 作者本人要做的邮箱、银行、计划任务配置 |
| `tests/fixtures/` | 脱敏后的真实账单样本（见其中的 README） |
